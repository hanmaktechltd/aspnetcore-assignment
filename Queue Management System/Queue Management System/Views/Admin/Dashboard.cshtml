@model DashboardViewModel

<h2>Admin Dashboard</h2>

<div class="mb-4">
    @Html.Partial("_ServicePointsPartial", Model.ServicePoints)
</div>

<div class="mb-4">
    @Html.Partial("_ServiceProvidersPartial", Model.ServiceProviders)
</div>

<div class="mt-4">
    <h4>Generate Analytical Report</h4>
    <div>
        <p>Generate an analytical report for your dashboard. Click the button below to download the report.</p>
        <button class="btn btn-primary" onclick="location.href='@Url.Action("DownloadReport", "Admin", @Model)'">Download Report</button>
    </div>
</div>

<div class="mt-3">
    <div class="d-flex">
        <form asp-action="Logout" asp-controller="Account" method="post">
            <button type="submit" class="btn btn-sm btn-secondary">Logout</button>
        </form>
    </div>
</div>
