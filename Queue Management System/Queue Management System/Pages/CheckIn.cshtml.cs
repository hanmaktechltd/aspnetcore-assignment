using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;

namespace Queue_Management_System.Pages
{
    public class CheckInModel : PageModel
    {
       /* private readonly ICustomerService _customerService;
        public CheckInModel(ICustomerService customerService)
        {
            _customerService = customerService;
        }
        public void OnGet()
        {
        }
        public IActionResult OnPost(string customerName, string serviceType)
        {
            if (!ModelState.IsValid)
            {
                return Page();
            }
            _customerService.CreateCustomer(customerName, serviceType);
            return RedirectToPage("/SuccessPage");
        }*/
    }
}
